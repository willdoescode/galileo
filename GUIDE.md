# Installation

1. Add the dependency to your `shard.yml`:

   ```yaml
   dependencies:
     galileo:
       github: willdoescode/galileo
   ```

2. Run `shards install`

# Usage

```crystal
require "galileo"
```